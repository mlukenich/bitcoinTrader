# ===================================================================
# Bitcoin Trading Bot Configuration
# ===================================================================

# --- Alpaca API Credentials ---
# !! IMPORTANT: Sign up at https://alpaca.markets/ to get your keys !!
# Use your PAPER TRADING keys for safe testing.
alpaca.api.key=PK9O2DZRFEGJ75TO3JJ7
alpaca.api.secret=b41rvDxXjhUbRnos8kdr7WlIEDjEQw3XfSW0Rcri
# The base URL for paper trading
alpaca.api.base-url=https://paper-api.alpaca.markets

# --- Trading Parameters ---
# The cryptocurrency symbol to trade (Alpaca format is BTC/USD)
trading.symbol=BTC/USD
# The short-term moving average period
trading.short-ma-period=10
# The long-term moving average period
trading.long-ma-period=50
# The percentage of total equity to risk per trade (e.g., 0.02 for 2%)
trading.risk.percentage=0.02
# Stop loss percentage
# The initial stop loss percentage, acts as a safety net
trading.initial-stop-loss.percentage=0.05

# --- ATR Stop-Loss Configuration ---
# The period for the Average True Range (ATR) calculation
trading.atr.period=14
# The multiplier for the ATR value to set the stop-loss (e.g., 2.0 * ATR)
trading.atr.multiplier=2.0

# --- Scheduler Configuration ---
scheduler.check-interval-ms=3600000

# --- Email Notification Settings ---
notification.email.enabled=false
notification.recipient.email=###

# --- Spring Mail Configuration (Example for Gmail) ---
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=####
spring.mail.password=ttsk szwr asvu xqmv
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# The base URL for Alpaca's market data API
alpaca.api.data-url=https://data.alpaca.markets

# The period for the Relative Strength Index (RSI) calculation
trading.rsi-period=14

# --- Advanced Order Management ---
# Enable or disable the take-profit feature
trading.take-profit.enabled=true
# The percentage gain at which to sell for a profit (e.g., 0.10 for 10%)
trading.take-profit.percentage=0.10

# Enable or disable the trailing stop-loss feature
trading.trailing-stop.enabled=true
# The percentage to trail below the highest price (e.g., 0.03 for 3%)
# Note: Your original 'stop-loss-percentage' will act as the initial stop-loss.
trading.trailing-stop.percentage=0.03

# File path for saving the bot's price history
bot.state.filepath=data/price_history.json

spring.security.user.name=admin
spring.security.user.password=admin

#Fake properties file for testing

